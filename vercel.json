{
  "builds": [
    {
      "src": "pyproject.toml",
      "use": "@vercel/python",
      "config": {
        "installCommand": "python -m pip install -e .",
        "outputDirectory": "/src"
      }
    },
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "corepack enable && yarn",
        "buildCommand": "node --run build",
        "outputDirectory": "/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/boot/boot/wsgi.py"
    }
  ]
}